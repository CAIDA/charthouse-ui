class ExpressionFactory {

    // TODO: try and handle serialized json gracefully
    // TODO: when parsing from canonical, remove newlines, collapse spaces and trim
    // TODO: creation funcs need to return array of expressions

    static createFromJson(json) {
    }

    static createFromCanonicalStr(expStr) {
        
    }

}

export default ExpressionFactory;
